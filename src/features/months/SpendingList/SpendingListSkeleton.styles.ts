import styled from "styled-components";

// Mirrors `StyledDay`'s stacked day number and month abbreviation, centred in
// the same 44px column.
export const StyledDayBlock = styled.div`
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 3px;
`;
